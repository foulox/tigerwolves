'use client'
// Placeholder — implemented in Task 8
import type { RunLeader } from '@/lib/data'

export default function RosterTab(_props: { runLeaders: RunLeader[]; runId: string; currentUserId: string }) {
  return null
}
