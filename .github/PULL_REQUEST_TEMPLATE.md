## Summary

[What changed and why — link the issue this closes.]

## Test plan

### Verified by Claude (automated)

- [ ] `npx tsc --noEmit`
- [ ] `npm run lint`
- [ ] `npm run test:unit`
- [ ] `npm run test:e2e` (if this PR touches a critical user flow)
- [ ] Preview deployment builds and the changed pages respond correctly

### Please verify manually (Lou)

[Specific screens/flows to click through as a signed-in leader on the Preview URL — this section stays unchecked. Automated coverage of the same flow does not replace it; the point is an independent second confirmation, not just test coverage. Only omit this section for changes with no user-facing surface (pure refactor, docs-only).]

- [ ] ...
